
{% if requirement.id is not null %}

<form action="{{ url('requirement_delete', {id: requirement.id}) }}" method="post" data-confirmation="true" id="delete-form">

    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />

    <button class="btn-flat btn btn-danger btn-sm pull-right" type="submit"><i class="fa fa-trash" aria-hidden="true"></i> Eliminar</button>

</form>

{{ include('requirement/_delete_requirement_confirmation.html.twig') }}

{% endif %}